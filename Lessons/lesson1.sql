SET SERVEROUTPUT ON;

/* 
DECLARE                                                                         it is used to initialis, create variables and create constants
    v_fname VARCHAR2(20);
    
BEGIN
    SELECT first_name
    INTO v_fname
    FROM HR.employees
    WHERE employee_id = 100;

    DBMS_OUTPUT.PUT_LINE('The first name of the employee is ' || v_fname);
END;
*/



